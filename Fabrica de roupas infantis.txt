CREATE DATABASE FAB_ROUPAS_INF
GO 
USE FAB_ROUPAS_INF
GO
CREATE TABLE ITENS(
	
	COD_TEC		INT IDENTITY,
	TIPO_TEC	VARCHAR(55),
	TAMANHO_TEC INT,
	COR_TEC		VARCHAR(55)

	CONSTRAINT PK_COD_TEC PRIMARY KEY (COD_TEC) 
		
)
GO
CREATE TABLE PRODUTOS(

	COD_PROD		INT IDENTITY,
	NOME_PROD		VARCHAR(55),
	TIPO_PROD		VARCHAR(55),
	TAMANHO_PROD	VARCHAR(55),
	COD_TEC			INT,
	DATA_PROD		DATE,
	HOUR_PROD		TIME,
	
	CONSTRAINT PK_COD_PROD PRIMARY KEY (COD_PROD),

	CONSTRAINT FK_COD_TEC FOREIGN KEY (COD_TEC)
		REFERENCES ITENS (COD_TEC)
)
GO
CREATE TABLE FUNCIONARIOS(
	
	COD_FUNC	INT IDENTITY,
	NOME_FUNC	VARCHAR(55) NOT NULL,
	SNOME_FUNC	VARCHAR(55) NOT NULL,
	DATA_ENT	DATE NOT NULL,
	DATA_SAI	DATE,

	CONSTRAINT PK_COD_FUNC	PRIMARY KEY (COD_FUNC)
)

GO 
ALTER TABLE PRODUTOS ADD PRECO MONEY 
GO
CREATE TABLE VENDA (

	COD_VENDA		INT IDENTITY,
	COD_PROD		INT,
	VALOR_TOT		MONEY,
	DATA_VEND		DATETIME,

	CONSTRAINT	PK_COD_VENDA PRIMARY KEY (COD_VENDA),
	CONSTRAINT FK_COD_PROD FOREIGN KEY (COD_PROD)
		REFERENCES PRODUTOS	(COD_PROD)
)
GO  
ALTER TABLE FUNCIONARIOS ALTER COLUMN SEXO VARCHAR(1) NOT NULL